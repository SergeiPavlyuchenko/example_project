package example.code.some_project.data.remote.di

import org.koin.dsl.module

// ToDo если дальше не понадобится - удалить. По сути remote dataSource это api, которые
//  лежат в другой дирректории. Используются в репозиториях, di для них - RepoModule
val dataSourceRemoteModule = module {

}